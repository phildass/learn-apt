# Learn Apt - Environment Variables Example
# Copy this file to .env.local and configure as needed

# ==============================================
# Application Configuration
# ==============================================

# Port the application will run on (default: 3000)
# PORT=3000

# Node environment (development, production, test)
# NODE_ENV=production

# ==============================================
# Domain Configuration
# ==============================================

# The application is pre-configured for learn-apt.iiskills.cloud
# No additional domain configuration is required unless
# you're deploying to a different domain

# ==============================================
# Supabase Configuration (REQUIRED)
# ==============================================

# Supabase Project URL
# Get this from your Supabase project settings > API > Project URL
# Example: https://xyzcompany.supabase.co
# IMPORTANT: Use the SAME Supabase project as the main iiskills-cloud repository
NEXT_PUBLIC_SUPABASE_URL=your-project-url-here

# Supabase Anonymous/Public Key
# Get this from your Supabase project settings > API > Project API keys > anon public
# This key is safe to use in the browser
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Site URL for OAuth redirects
# Development: http://localhost:3000
# Production: https://learn-apt.iiskills.cloud
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Main domain for production (used for cross-subdomain authentication)
# This allows authentication to work across all *.iiskills.cloud subdomains
NEXT_PUBLIC_MAIN_DOMAIN=iiskills.cloud

# Cookie domain for cross-subdomain authentication
# Use dot prefix to share cookies across all subdomains
# Example: .iiskills.cloud (note the leading dot)
# Leave undefined or commented out for localhost development
NEXT_PUBLIC_COOKIE_DOMAIN=.iiskills.cloud

# Admin email addresses (comma-separated)
# Users with these emails will have admin access
# NEXT_PUBLIC_ADMIN_EMAILS=admin@iiskills.cloud,phil@iiskills.cloud

# ==============================================
# Authentication (Legacy - Being Replaced)
# ==============================================

# Only needed if authentication features are enabled

# NextAuth Configuration
# Generate secret with: openssl rand -base64 32
# NEXTAUTH_URL=https://learn-apt.iiskills.cloud
# NEXTAUTH_SECRET=your-secret-key-here

# Admin Credentials (Legacy - use Supabase instead)
# ADMIN_PASSWORD=your-secure-password-here
# Admin Credentials (Fallback when Supabase is not configured)
# ⚠️ WARNING: This is only for development/demo purposes
# The password will be exposed in the client-side bundle
# For production, always use Supabase authentication instead
# NEXT_PUBLIC_ADMIN_PASSWORD=your-secure-password-here

# Supabase Configuration
# Get these from your Supabase project settings
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ==============================================
# Analytics (Optional)
# ==============================================

# Google Analytics
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Vercel Analytics (automatically enabled on Vercel)
# No configuration needed

# ==============================================
# Notes
# ==============================================

# - Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# - Other variables are only available on the server side
# - Never commit .env.local or .env.production to version control
# - Use .env for default values shared across all environments
# - Use .env.local for local development overrides
# - Use .env.production for production-specific values
# - IMPORTANT: Use the SAME Supabase project credentials as the main iiskills-cloud repository
#   to ensure users can authenticate across all subdomains
